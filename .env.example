# Optional: If you want to bypass the reverse proxy and call the backend directly
# (Default is empty, which uses the relative '/api' proxy configured in vite.config.js / nginx.conf)
# VITE_API_BASE_URL=http://localhost:8000/api
